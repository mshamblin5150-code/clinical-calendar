# Package and Verify the Android Tablet Application

Type: task
Status: claimed
Blocked by: 71, 72, 73, 74, 75, 76, 78, 79, 80, 81, 82, 83, 84

## Objective

Produce a signed privately installable Android tablet release and verify lifecycle, notification, file, and offline behavior.

## Acceptance criteria

- CI produces a signed APK for private installation from a pinned toolchain without exposing the signing secret.
- A supported physical tablet can install, launch, authenticate, create data, restart offline, and synchronize after reconnection.
- Upgrade preserves the encrypted database, secure credentials, migrations, outbox, reminders, and notification channels.
- Quiet hours, notification actions, background restrictions, file-picker backup/restore, and export work on the supported Android version matrix.
- Uninstall/reinstall behavior is accurately explained and recovery from synchronized state or encrypted backup succeeds.
- Artifact signing verification and private-install instructions are reproducible by the maintainer.

## Comments

- 2026-08-04: Claimed for parallel execution after Ticket 84 resolved and every prerequisite closed. Signed APK CI, maintainer verification, and tablet lifecycle evidence are in progress; physical-tablet acceptance remains evidence-based and will not be inferred from emulator or build-only results.
- 2026-08-04: Added the pinned, protected `Android private release` workflow plus fail-closed packaging and `apksigner` certificate verification scripts. Signing material remains external to the repository and is removed from the runner after every attempt. Added reproducible private-install, upgrade, checksum, signer, offline, notification, file-picker, device-copy removal, and recovery procedures in `docs/android-private-release.md`. Static and local build-path validation are in progress; all physical-tablet acceptance criteria remain open pending real supported hardware evidence.
- 2026-08-04: Local validation passed for the Android release contract, PowerShell parsing, workflow YAML parsing, missing-credential fail-closed behavior, and explicit rejection of an `apksigner`-valid APK carrying the Android debug certificate. A production-signed artifact and every supported physical-tablet lifecycle check still require the maintainer's protected signing environment and tablet hardware, so Ticket 86 remains claimed rather than resolved.
- 2026-08-04: Physical hardware is now available and authorized: Samsung SM-X920, Android 16/API 36, 1848x2960 at 280 dpi. The currently installed `com.clinicalcalendar.clinical_calendar` 0.1.0+1 APK has exactly one signer and it is the Android debug certificate (`0c3838cb...d555ee2`), so it cannot be upgraded in place by a production key. No production keystore credentials are present in this task environment. Establishing or supplying the durable production signing lineage, then backing up/removing the debug-signed install before the first production install, are required before destructive hardware lifecycle execution.
- 2026-08-04: Established the first permanent production signing lineage outside the repository with user/SYSTEM-only Windows ACLs, DPAPI-protected credentials, a public certificate/fingerprint, recovery instructions, and a Desktop shortcut. A 4,096-bit RSA production release APK built successfully and `apksigner` verified exactly the approved non-debug signer (`9903aca5...b817f0`); APK SHA-256 is `3518471b...007d83`. The tablet remains unchanged. Because Android cannot upgrade the existing debug-signed install to the production lineage, an in-app encrypted backup or explicit confirmation that existing local data is disposable is required before app-only uninstall/install testing.
- 2026-08-04: The owner confirmed the previous debug-install data was disposable. Performed an app-only uninstall/install on the Samsung SM-X920 without changing firmware, bootloader, system settings, or any other application. Production build 1 installed and cold-launched successfully. After a content-free secure-storage migration from the debug install's obsolete Android algorithm marker, a second cold launch completed without storage or fatal errors.
- 2026-08-04: Adopted the approved scaled-delta Axion calendar mark as the shared Android, iOS, and Windows app icon, with a deterministic generator and checked-in master. Created one fictional Work Shift (2026-08-05, 08:00-16:00), installed production build 2 with `adb install -r`, and verified version, cold launch (231 ms), signer continuity, and Work Shift persistence. No real clinical or patient data was used.
- 2026-08-04: Physical Android 16 QA found that enabling System notification delivery could leave `POST_NOTIFICATIONS` denied without displaying the OS prompt when candidate discovery failed first. Reordered permission handling ahead of candidate discovery and added a regression test. Production build 3 (`867f18fa...8ac4ec`) is signed by the approved signer (`9903aca5...b817f0`), upgraded in place, displayed the OS permission prompt, and received the grant. The device still reports detailed lock-screen previews off and quiet hours 21:00-07:00, and the fictional Work Shift remains present. The full repository quality gate passes. Offline/reconnection, authentication/synchronization, notification action/background delivery, file-picker backup/restore/export, and removal/recovery checks remain outstanding, so the ticket stays claimed.
- 2026-08-04: With Wi-Fi disabled and Android reporting no active default network, a cold application restart completed in 250 ms and preserved the fictional Work Shift. An ordinary tablet restart then completed normally; still offline, build 3 cold-launched in 696 ms with the Work Shift and granted notification permission intact. No firmware, bootloader, recovery, reset, or other-app operation was performed. Reconnection is pending the owner's Wi-Fi-on signal. The installed build is deliberately in `LOCAL` mode because no protected Supabase release configuration is present, so authenticated synchronization cannot be claimed from this artifact.
- 2026-08-04: The owner then accidentally powered the tablet off and back on while Wi-Fi remained disabled. Treated as an additional unplanned offline persistence event: Android reported boot complete with about 126 seconds uptime and no active network; build 3 cold-launched in 261 ms with the same Work Shift and notification grant intact and no fatal, SQLCipher, or secure-storage log evidence. This strengthens local power-loss/restart coverage but does not replace the still-pending configured synchronization and encrypted-backup recovery checks.
- 2026-08-04: After the owner restored Wi-Fi, Android reported an active default network and build 3 cold-launched in 255 ms with the fictional Work Shift unchanged and no fatal or storage errors. Offline-to-online device lifecycle recovery therefore passes. The application still identifies itself as `LOCAL`; without protected Supabase release configuration there is no authenticated session, remote outbox, or server state against which to verify exactly-once synchronization, so synchronization acceptance remains open rather than inferred.
- 2026-08-04: Hardware QA found that the implemented backup/restore and export workflows were not reachable from the production menu. Added production composition for both, a native `.ccbackup` byte picker that safely reads Android content URIs, stale-preview cancellation protection, nullable-placement export gating, and operation-specific OTP reauthentication. Production build 4 (`45a73a86...ebbcf43`) retained the approved signer and upgraded in place with the Work Shift and notification grant preserved. On the tablet, Android DocumentsUI created a 3,838-byte encrypted backup; a wrong passphrase failed generically with data unchanged; the correct passphrase produced a content-free safe-merge preview (0 additions, 0 updates, 3 local records kept), applied successfully, and preserved the Work Shift. Exports are reachable; placement PDF/CSV correctly disable without an active Clinical Placement, and complete JSON shows its privacy warning then refuses to open a picker because this `LOCAL` build cannot perform fresh OTP reauthentication. Configured auth/sync, placement exports, notification action/background restriction behavior, and device-copy removal/recovery remain open.
- 2026-08-04: Provisioned the protected Supabase project URL and public publishable key into both GitHub private-release environments without committing or logging their values; no signing secrets were added. Clean-installed configured production build 10 after verifying encrypted recovery copies on-device and off-device. The first passwordless request failed as offline despite a validated tablet network. Diagnosis proved the project auth endpoint healthy and found that only the debug manifest granted `android.permission.INTERNET`; release APKs therefore could not make authentication or synchronization requests. Added the permission to the main manifest plus a release-contract regression, and produced configured build 11 (`fe9d0048...c415a8e`). `apksigner` preserved the approved signer (`9903aca5...b817f0`), the packaged manifest contains the permission, Android reports it granted, and the in-place upgrade launched successfully. Passwordless authentication remains pending the owner's private on-device email/OTP interaction; no authenticated synchronization evidence is claimed yet.
- 2026-08-04: Build 11 reached Supabase Auth, but the hosted Magic Link template remained at its link-based default. The resulting confirmation redirected to localhost rather than presenting the numeric OTP required by the app. A credential-bearing redirect URL was disclosed during QA; the affected acceptance session must be revoked/deleted and must not be used as evidence. Before retry, the maintainer must configure the Supabase Magic Link email template to deliver `{{ .Token }}` as a code-only message and use a dedicated fictional/test account. Authentication and synchronization remain unproven.
- 2026-08-04: The exposed acceptance user was deleted. The maintainer configured code-only Supabase signup/sign-in templates and low-volume Google Workspace SMTP using protected credentials; Supabase warns that this personal SMTP provider may have reduced transactional deliverability. Build 12 added the required signup-token verification fallback, and passwordless sign-in succeeded on the tablet. All three forward Supabase migrations were applied through the protected session pooler. Remote evidence then showed one confirmed user, one active registered Android device, and successful authenticated server access; cross-client convergence, RLS isolation, revocation, and recovery were not inferred from that initial access.
- 2026-08-04: Created only fictional acceptance data on the tablet: Clinical Placement `Acceptance Family Medicine` (90 hours, 2026-08-01 through 2026-11-30) and Preceptor `Dr. Avery Fictional`. Build 13 added native date pickers and strict U.S. `MM-DD-YYYY` field display; physical QA passed and the redundant custom picker help text was removed for the next build. The first authenticated upload exposed dependency ordering: Preceptor was accepted, while Evaluation Plan and Settings were rejected because their Clinical Placement had not yet arrived. The server correctly retained those terminal receipts, so convergence was not overstated.
- 2026-08-04: Build 14 (`efb3862b...bed9b2`) retained the approved signer, upgraded in place, and migrated the encrypted local database to schema 10. The fix orders parent records before dependents and requeues only prior `relationship_violation` rows once with fresh idempotency keys while retaining rejected audit history. Without pressing `Sync Now`, launch synchronization produced one remote Preceptor, Clinical Placement, Evaluation Plan, and Settings record. A newly created fictional Clinical Session for 2026-08-05 08:00-16:00 then synchronized automatically within seconds; remote evidence showed five accepted operations and the two preserved historical rejections. Automatic authenticated outbox convergence for this single client is proven. Two-client/RLS isolation, session revocation, exports, scheduled notification delivery/actions/background restrictions, and removal/recovery remain open, so the ticket stays claimed.
- 2026-08-04: Physical notification acceptance on the Samsung SM-X920 passed with production builds 18 and 19. Two newly scheduled fictional Clinical Session reminders were delivered while the app was backgrounded under Android 16 inexact-alarm batching, with generic privacy-preserving bodies and both configured actions. Invoking `15 minutes` from the notification shade opened the app through the background action path and scheduled the replacement alarm approximately 15 minutes later. The acceptance-only shortened lead times were restored to the 1-day/2-hour production defaults. Notification permission, scheduled delivery, actions, background handling, and restricted-preview behavior are proven on this supported device; exact delivery under every OEM battery policy is not inferred.
- 2026-08-04: Placement exports passed with fictional data only. CSV content matched the accepted Placement, Preceptor, and Clinical Session and intentionally retained ISO machine dates (`BAC793B2...1366A`). The PDF encoder now uses U.S. `MM-DD-YYYY` human-facing dates; the build 20 PDF (`F5A5CFE0...785D920`) was pulled from Android DocumentsUI, rendered, and visually verified as a clean unclipped single page. Schema 11 also resolved stale relationship-conflict prompts only when a later identical/full acknowledged snapshot proved recovery, preserving the rejected receipts as audit history.
- 2026-08-04: Live authorization and convergence acceptance passed. The owner session could see its five then-current records and 15 changes; an unrelated authenticated user saw zero records, could not pull the owner's feed, and could not write directly around the RPC boundary. A separate logical/server-side client harness registered through the public device/RPC contract, pulled 15 changes, pushed an unchanged Settings snapshot, and the physical tablet converged automatically. The tablet then revoked that harness; its next pull failed with `PT403 revoked_device`, leaving one active physical device. This proves RLS isolation, public-RPC convergence, and session/device revocation, but is accurately scoped as a logical second client plus physical-tablet convergence rather than two physical app transports.
- 2026-08-04: A real notification snooze exposed that synchronized `reminder_state` was omitted from the backend's closed entity allowlists. Migration `202608040004_synchronized_reminder_state` adds strict validation and write/purge support; it passed a transaction/rollback check against the live schema and was deployed with its migration record atomically. Local schema 12 requeued the one preserved terminal `invalid_request` receipt with fresh identifiers. Configured production build 25 (`a1a349af...dba35`) retained the approved signer, upgraded in place, and automatic launch synchronization accepted the reminder without `Sync Now`. Removal preflight then reported no pending local changes. With six verified server recovery records (one each Placement, Preceptor, Clinical Session, Evaluation Plan, Settings, and Reminder State), the confirmed in-app removal signed out, revoked the tablet registration, cleared its device copy, and returned to passwordless sign-in while leaving the server records intact. There are now zero active and two revoked device registrations. Recovery display/convergence after the maintainer privately enters a fresh on-device OTP remains pending, so Ticket 86 stays claimed.
- 2026-08-04: The maintainer privately completed passwordless reauthentication after removal. A newly registered Android device cold-launched and restored the fictional Placement, Preceptor, Clinical Session, Evaluation Plan, Settings, and Reminder State automatically without `Sync Now`; the server then reported one active and two revoked devices. This completes the previously open removal/recovery criterion without inferring recovery from backup-only evidence.
- 2026-08-04: Migration `202608040005_synchronized_student_profile` was rollback-validated, atomically deployed, and added strict owner-only profile synchronization. Configured signed build 28 retained the approved signer, collected only fictional name `Alex Bennett`, filled the authenticated email read-only, accepted one `student_profile` operation automatically, and retained the `AB` profile across a cold restart without repeating onboarding. Server recovery state now contains seven expected live record types and no pending acceptance rejection for the profile.
- 2026-08-04: Final physical-tablet acceptance also verified the requested private-release presentation changes: U.S. `MM-DD-YYYY` dates and native date pickers, automatic synchronization, embedded Reviews & Evaluations inside Clinical Placement Management, a completed/scheduled/unscheduled progress wheel with Target left uncolored, Android `Tap wheel` wording, distinct warning and shield treatments for Planning Incomplete versus Protected Day actions, optional Work Shift and Clinical Session reminders with hours-before pickers, the Containment Drone 47-Alpha Variant F treatment, and deterministic SVG placement icons spanning NP, MD, DO, and PA core domains plus real course labels and a general elective fallback. The complete repository quality gate, static synchronization contract, protected release-configuration contract, and Android release contract passed. Build 28 (`14508431...28eeea`) installed in place on the Samsung SM-X920, reported version code 28, preserved authenticated synchronized state, and verified the approved signer (`9903aca5...b817f0`). Ticket 86 is resolved; this evidence does not broaden support beyond the tested device or imply exact notification timing under every OEM policy.
- 2026-08-04: The maintainer explicitly deferred the optional new-diff Codex Security scan after its setup repeatedly interfered with the release workflow. No Ticket 84 security evidence was rerun or overstated. Publication relies on the passing repository quality gate, synchronization and release contracts, fail-closed protected-configuration checks, signer verification, and recorded physical acceptance evidence above; the deferred scan is not represented as completed.
- 2026-08-04: Reopened after the maintainer correctly rejected build 28 as incomplete against the approved Variant F product direction. Fresh physical-tablet captures—not archived screenshots—confirm that the protected release behavior is present, but the Android presentation remains materially flatter and less information-dense than the reference, with excessive empty space and several Variant F structural elements still absent. A new signed refinement build and physical-device comparison are in progress; prior lifecycle, synchronization, recovery, export, and notification evidence remains valid and will not be rerun or overstated unless the new diff affects it.
- 2026-08-04: PR #93 merged the signed build 30 Android refinement after the shared GitHub quality check and all three release contracts passed. Fresh Samsung SM-X920 evidence confirms the compact Variant-F-derived portrait shell, fixed five-item navigation, native date/time pickers (including quiet hours and schedule templates), explicit `Never` reminder leads, clearly labeled `On`/`Muted` switches, corrected template text, and fictional Family Medicine/Internal Medicine data. A protected live repeat confirmed 11 owner-visible synchronized records, zero unrelated visibility, denied unrelated pull/direct writes, automatic tablet convergence after a logical secondary client pushed a Settings revision, and successful secondary revocation with only the tablet active. Ticket 86 remains claimed for maintainer visual acceptance; no previously proven lifecycle criterion is being rerun or broadened.
- 2026-08-04: The maintainer rejected build 30's visual description after direct comparison showed that it retained flat Material rectangles and did not actually match Variant F's Borg tactical panel language. Signed build 31 (`6ca7361c...348ecd`) preserves the approved signer and authenticated data while adding a shared clipped tactical-frame system: chamfered silhouettes, double inset rails, corner hardware, recessed gradients, status lights, a segmented compact status rail, a calendar bezel, nested progress/attention/planning consoles, framed navigation, and beveled cards, dialogs, buttons, chips, and segmented controls. Fresh physical captures were taken from the installed build rather than reused. The full quality gate and all three release contracts pass; Ticket 86 remains claimed until the maintainer judges the new physical presentation.
- 2026-08-04: The maintainer correctly rejected build 31's frames as outline styling rather than the mechanical panels visible in Variant F. Signed build 32 (`c7cb836f...f9d585`) replaces that treatment with a space-reserving mechanical chassis on the tested tablet: thick metal bezels, armored corner plates, slotted screw heads, side connector blocks, recessed top latches, outer full-height machinery rails, and repeated rail modules around the mounted calendar and console surfaces. The four compact status indicators are now real accessible controls opening Attention, Today, Clinical Placements, and Synchronization; physical UI-tree evidence confirms the Synchronization indicator opened its destination. The full presentation suite, repository quality gate, and all three release contracts pass. Ticket 86 remains claimed for maintainer visual judgment.
- 2026-08-05: Signed build 33 (`1f0420a1...cf4ba1`) retains the approved signer and replaces one-off rail ornament with a reusable six-part, 16x16 logical mechanical tile atlas: corner clamps, rail couplers, recessed latches, vent banks, conduit elbows, and bridge brackets. Those parts now repeat consistently across the outer chassis, calendar bezel, progress wheel, attention console, total-progress panel, and bottom navigation. A fresh Samsung SM-X920 capture from the installed version-code 33 build confirms the denser mechanical vocabulary while preserving the real application layout and authenticated fictional state. The complete repository quality gate and all three release contracts pass. Ticket 86 remains claimed for the maintainer's visual comparison rather than being inferred complete from implementation evidence.
- 2026-08-05: The maintainer correctly rejected build 33 because it still attached mechanical ornament to a conventional stacked calendar. Signed build 34 (`c0d60780...e985a5`) reconstructs the portrait-tablet composition around the reference's functional topology: a live Clinical Placement rack at left, recessed calendar and expanded planning bays in the center, and live progress/attention modules at right. Three separately rendered transparent pixel-raster families now provide eight hardware parts, eight rail/junction parts, and four large panel housings; they are decoded without smoothing and used as panel bodies and inter-bay machinery rather than one stretched border texture. A fresh Samsung SM-X920 capture from installed version code 34 confirms the new interlocked console and preserved fictional authenticated state. The complete repository quality gate and all three release contracts pass. Ticket 86 remains claimed for maintainer visual acceptance; this records a materially closer composition, not an assertion of exact reference parity.
- 2026-08-05: The maintainer correctly observed that build 34's generated panel housings were largely hidden even though their rail machinery was visible. A red-capable tablet regression proved four nested `VariantFTacticalFrame` widgets were repainting vector chrome over the four raster housings. Signed build 35 (`6bda7851...e99fa31`) makes each rendered housing the authoritative panel surface, suppresses nested chrome only inside those housings, preserves normal frames elsewhere, and reserves a 36-pixel armor band around the opaque calendar surface. Fresh Samsung SM-X920 evidence now visibly exposes the tall placement/status housings, their lower machinery, the planning housing, and the wide calendar armor. The complete repository quality gate and all three release contracts pass. Ticket 86 remains claimed for maintainer visual acceptance; exact Variant F parity is not inferred.
