# Rejected candidate v3 (`a28bdb8`)

The maintainer accepted the full-bleed chassis direction but rejected this
candidate's command buttons, bottom navigation, and Family Medicine progress
instrument. The comparison and manifest in this directory are immutable
historical evidence, not acceptance targets.

The matching deterministic reference set and Gallery images are archived at
`packages/clinical_calendar_presentation/test/baselines/containment_drone_v2/rejected/a28bdb8/`.
