name: clinical_calendar_local_data
description: Local encrypted persistence adapter boundary.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  clinical_calendar_application:
    path: ../clinical_calendar_application
  clinical_calendar_domain:
    path: ../clinical_calendar_domain
  cryptography: ^2.9.0
  sqlite3: ^3.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

hooks:
  user_defines:
    sqlite3:
      source: sqlcipher
