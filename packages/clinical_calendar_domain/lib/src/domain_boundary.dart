/// Identifies the innermost package boundary without defining ticket 60's
/// domain model prematurely.
abstract final class DomainBoundary {
  static const String packageName = 'clinical_calendar_domain';
}
