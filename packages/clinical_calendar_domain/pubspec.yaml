name: clinical_calendar_domain
description: Pure Dart domain boundary for Clinical Calendar.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
