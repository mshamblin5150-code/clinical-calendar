# Rejected candidate v3 (`a28bdb8`)

This directory preserves the deterministic Containment reference collection
and Gallery evidence that existed when the maintainer rejected candidate v3's
command buttons, navigation keys, and progress instrument. It must not be used
as an acceptance baseline or overwritten by later candidates.
